export enum CategoryTab {
  Home,
  General,
  Business,
  Health,
  Science,
  Technology,
  Sports,
}

export enum SidebarTab {
  Latest,
  Favorites,
}

export enum MobileTab {
  Featured,
  Latest,
}
